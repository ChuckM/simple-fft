Notes
----

Here in the misc directory. To run the python code you'll need to apt install

sudo apt install python3-numpy python3-scipy python3-matplotlib

So `perfect_refs` are not so perfect.

Given that `sqrt(x^2 + y^2) = a`
if we move `y` from 0 to 11585 then that should be the
best we can do.

```
x^2 + y^2 = a^2
x^2 = a^2 - y^2
x = sqrt(a^2 - y^2)
```


